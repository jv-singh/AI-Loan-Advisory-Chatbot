# ──────────────────────────────────────────────────────────────────────────────
# Loan Advisory Agent — Python Dependencies
# Install with: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # file upload support

# ── AI / LLM ──────────────────────────────────────────────────────────────────
langchain-core==1.4.8
langchain==1.3.11
langchain-classic==1.0.8          # compat shim for legacy langchain.chains/text_splitter imports
langchain-groq==1.1.0             # Groq LLM (free tier) — OpenAI-compatible chat
langchain-huggingface==1.0.0      # local sentence-transformer embeddings (no API key)
langchain-openai==1.3.3           # kept as a fallback if you want to swap back
langchain-community==0.4.2
langgraph==1.2.7                  # multi-agent orchestration (v1.2 line)
langchain-chroma==1.1.0           # vector store integration
langchain-text-splitters==1.1.2   # canonical text splitter path (langchain 1.x)

# ── Embeddings & Vector Store ─────────────────────────────────────────────────
chromadb==1.3.5                  # local vector DB (>=1.3.5 required by langchain-chroma 1.1)
sentence-transformers==3.0.1     # local embedding fallback

# ── Document Processing ───────────────────────────────────────────────────────
pypdf==4.3.1                     # PDF ingestion
python-docx==1.1.2               # DOCX ingestion (used by some loaders)
docx2txt==0.9                    # DOCX ingestion (required by langchain Docx2txtLoader)
unstructured==0.15.7             # universal document parser

# ── Database ──────────────────────────────────────────────────────────────────
supabase==2.7.4                  # Supabase Python client
sqlalchemy==2.0.35               # ORM for local dev fallback
psycopg2-binary==2.9.9           # PostgreSQL driver

# ── Data & Validation ─────────────────────────────────────────────────────────
pydantic==2.9.0
pydantic-settings>=2.10.1,<3.0.0
pandas==2.2.2
faker==28.1.0                    # synthetic data generation

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.2
tenacity==8.5.0                 # retry logic
structlog==24.4.0                # structured logging
rich==13.8.1                     # pretty CLI output

# ── Frontend (React + Vite) ──────────────────────────────────────────────────
# Frontend lives in ./frontend — see frontend/package.json for JS dependencies.
# Run with:  cd frontend && npm install && npm run dev   (http://localhost:5173)
# (The legacy Streamlit UI has been replaced by the React + Three.js app.)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2                    # async test client